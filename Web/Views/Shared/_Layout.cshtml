<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="shortcut icon" type="image/x-icon" href="~/Img/favicon.ico" />

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/xlsx/0.18.5/xlsx.full.min.js"></script> <!-- Generar Excel -->
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/js/bootstrap.bundle.min.js"
            integrity="sha384-MrcW6ZMFYlzcLA8Nl+NtUVF0sA7MsXsP1UyJoMp4YLEuNSfAP+JcXn/tWtIaxVXM"
            crossorigin="anonymous"></script>

    <link href="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/css/select2.min.css" rel="stylesheet" />
    <script src="https://cdn.jsdelivr.net/npm/select2@4.1.0-rc.0/dist/js/select2.min.js"></script>

    <script src="https://cdn.datatables.net/1.10.18/js/jquery.dataTables.min.js"></script>
    <link rel="stylesheet" type="text/css" href="https://cdn.datatables.net/1.10.18/css/jquery.dataTables.min.css">

    <script src="~/Scripts/ASP.NET_SignalR_2.4.2.js"></script>
    @*<script src="https://cdnjs.cloudflare.com/ajax/libs/signalr.js/2.4.2/jquery.signalR.min.js"></script>*@

    <title>Ejemplos Prácticos</title>

    <style>
        /* ============================================================================================================ */
        /* Select 2 */

        /* Establece la altura deseada para el componente Select2 */
        .select2-container--default .select2-selection--single {
            height: 38px; /* Puedes ajustar la altura según tus necesidades */
        }

        /* Establece la altura deseada para las opciones desplegables del Select2 */
        .select2-container--default .select2-results__option {
            height: 38px; /* Puedes ajustar la altura según tus necesidades */
        }

        /* Establece la altura deseada para las opciones seleccionadas del Select2 (múltiple) */
        .select2-container--default .select2-selection__choice {
            height: 38px; /* Puedes ajustar la altura según tus necesidades */
        }

        /* Centrar verticalmente el texto dentro del Select2 */
        .select2-container--default .select2-selection--single .select2-selection__rendered {
            display: flex;
            align-items: center;
            height: 100%;
        }

        /* Ajusta el z-index del menú desplegable de Select2 */
        .select2-container--default .select2-dropdown--below {
            z-index: 5000;
        }

        .select2-container--default .select2-dropdown--above {
            z-index: 5000;
        }

        /* ============================================================================================================ */
    </style>
    <script>
        function obtenerDatos(datos) {
            return new Promise((resolve, reject) => {
                fetch(datos.url, {
                    method: "POST",
                    headers: { "Content-Type": "application/x-www-form-urlencoded" },
                    body: datos.queryString,
                })
                    .then(response => {
                        if (response.ok) {
                            response.json().then(res => {
                                if (res.status == 200)
                                    resolve(res.objeto);
                                else
                                    reject(res);
                            });
                        }
                    })
                    .catch(error => {
                        reject("Ocurrió un error al obtener la información");
                    });
            });
        }
    </script>
</head>

<body>

    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="#">Ejemplos Prácticos</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav">
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Home")">Inicio</a>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            DescargaPDF
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="@Url.Action("Index", "OpenHtmlToPdf")">OpenHtmlToPdf</a></li>
                            <li><a class="dropdown-item" href="@Url.Action("Index", "PdfConGo")">PDF con exe (Go)</a></li>
                        </ul>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Select 2
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="@Url.Action("Index", "Select2")">Simple</a></li>
                            <li><a class="dropdown-item" href="@Url.Action("Index", "Select2Complejo")">Complejo</a></li>
                        </ul>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "EjemploCheckbox")">Ejemplo Checkbox</a>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            Descarga Archivos
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="@Url.Action("Index", "EjemploDescargaExcel")">Excel</a></li>
                            <li><a class="dropdown-item" href="@Url.Action("Index", "EjemploDescargaCsv")">Csv</a></li>
                        </ul>
                    </li>


                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            DataTable
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="@Url.Action("Index", "DataTable")">Simple</a></li>
                            <li><a class="dropdown-item" href="@Url.Action("Index", "DataTableComplejo")">Complejo</a></li>
                        </ul>
                    </li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
                            SignalR
                        </a>
                        <ul class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <li><a class="dropdown-item" href="@Url.Action("ProbandoEnviarMensaje", "EjemploSignalR")">Probando Enviar Mensaje</a></li>
                            <li><a class="dropdown-item" href="@Url.Action("ProbandoRecibirMensaje", "EjemploSignalR")">Probando Recibir Mensaje</a></li>

                            <li><a class="dropdown-item" href="@Url.Action("Index", "EjemploSignalR", new { rut = "1234" })" target="_blank">Prueba 1</a></li>
                            <li><a class="dropdown-item" href="@Url.Action("Index", "EjemploSignalR", new { rut = "5678" })" target="_blank">Prueba 2</a></li>
                        </ul>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "SubirExcel")">Subir Excel</a>
                    </li>

                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "Encriptar")">Encriptar</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="@Url.Action("Index", "EjemploJwt")">Jwt</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>


    <div class="container-fluid">
        @RenderBody()
    </div>

    <script src="~/Scripts/xlsx.full.min.js"></script>

    <script>

        function validarNuloVacio(contenido)
        {
            if (contenido == null || contenido == "") return false;
            return true;
        }

        function htmlEncoder(cadena)
        {
            cadena = cadena.replace(/&/g, '/amp/');  // Reeemplazar el &
            cadena = cadena.replace(/</g, '/lt/');
            cadena = cadena.replace(/>/g, '/gt/');
            return cadena;
        }

        function toQueryString(objeto)
        {
            const encode = (value) =>
            {
                if (value != null && typeof value === "string")
                    return encodeURIComponent(this.htmlEncoder(value));

                return encodeURIComponent(value);
            };

            return Object.entries(objeto)
                .map(([key, value]) => {
                    if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
                        return `${encodeURIComponent(key)}=${encode(value)}`;
                    }
                    else {
                        return "";
                    }
                })
                .filter(x => x !== "")
                .join('&');
        }

        function llenarSelect(datos)
        {
            // llenarSelect({ querySelector: "#modalAtributo [name='tagHtml']", data: [{ codigo: "1", descripcion: "1" }], selectedValue: 1 });

            const retornoEvento = () => {
                let res = { ...datos };
                res.value = $(datos.querySelector).val();
                res.text = $(`${datos.querySelector} option:selected`).text();
                return res;
            };

            if (Array.from($(datos.querySelector + ' option[value!=""]')).length)   // Si tiene elementos excepto el seleccione
                limpiarSelect(datos.querySelector);                              // Esto sirve al refrescar el combobox con nuevos datos

            quitarSeleccionSelect(datos.querySelector);

            if (datos.data != null)
            {
                if (datos.selectedValue == null)
                {
                    datos.data.forEach(c => {
                        $(datos.querySelector).append('<option value="' + c.codigo + '">' + c.descripcion + '</option>');
                    });
                }
                else
                {
                    if (Array.isArray(datos.selectedValue))
                    {
                        var arreglo = datos.selectedValue;

                        datos.data.forEach(c => {
                            let selected = (arreglo.includes(c.codigo.toString())) ? "selected" : "";
                            $(datos.querySelector).append('<option value="' + c.codigo + '" ' + selected + '>' + c.descripcion + '</option>');
                        });
                    }
                    else {
                        datos.data.forEach(c => {
                            let selected = (c.codigo == datos.selectedValue) ? "selected" : "";
                            $(datos.querySelector).append('<option value="' + c.codigo + '" ' + selected + '>' + c.descripcion + '</option>');
                        });
                    }
                }

            }

            $(datos.querySelector).on("change", () =>
            {
                if (datos.onchange != null && typeof datos.onchange === "function") {
                    let retorno = retornoEvento();  // { value: "1234", text: "Hola" }
                    datos.onchange(retorno);
                }
            });

            //$(datos.querySelector).trigger("chosen:updated");   // El select toma los cambios realizados  (MDBOOTSTRAP)
        }

        function quitarSeleccionSelect(querySelector) 
        {
	        $(querySelector).find('option:first-child').prop('selected', true).end();
            //$(querySelector).find('option:first-child').prop('selected', true).end().trigger('chosen:updated');   // (MDBOOTSTRAP)
        }

        function limpiarSelect(querySelector) {
            $(querySelector + ' option[value!=""]').remove().end();     // Remueve todos los options excepto el seleccione
            // $(querySelector).trigger("chosen:updated");              // (MDBOOTSTRAP)
        }

        function generarExcel(data, nombreArchivo)
        {
            let workbook = XLSX.utils.book_new(),
                worksheet = XLSX.utils.aoa_to_sheet(data);

            workbook.SheetNames.push("Hoja 1");
            workbook.Sheets["Hoja 1"] = worksheet;

            let xlsbin = XLSX.write(workbook, {
                bookType: "xlsx",
                type: "binary"
            });

            let buffer = new ArrayBuffer(xlsbin.length),
                array = new Uint8Array(buffer);

            for (let i = 0; i < xlsbin.length; i++) {
                array[i] = xlsbin.charCodeAt(i) & 0XFF;
            }

            let xlsblob = new Blob([buffer], { type: "application/octet-stream" });
            delete array; delete buffer; delete xlsbin;

            let url = window.URL.createObjectURL(xlsblob),
                anchor = document.createElement("a");
            anchor.href = url;
            anchor.download = `${nombreArchivo}.xlsx`;
            anchor.click();
            window.URL.revokeObjectURL(url);
            delete anchor;
        }

    </script>

</body>
</html>
